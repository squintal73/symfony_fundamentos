 <div class="col-sm-12">
   <form action="" method="POST">
     <div class="form-group">
     <label for="title">Titulo</label>
     <input type="text" class="form-control" name="title" placeholder="Titulo" value="{{ task.title ?? "" }}" >
     </div>
     <div class="form-group">
     <label for="description">Descricao</label>
     <textarea class="form-control" name="description" placeholder="Descricao" >{{ task.description ?? ""}}</textarea>
     </div>
     <button type="submit" class="btn btn-primary"> Gravar </button>
    </form>
  </div>